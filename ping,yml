# playbook.yml

---
- name: Ping Servers
  hosts: my_servers
  gather_facts: false

  tasks:
    - name: Ping Server01
      ping:
      when: inventory_hostname == 'server01'

    - name: Ping Server02
      ping:
      when: inventory_hostname == 'server02'
